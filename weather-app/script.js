const f=document.getElementById("f"),city=document.getElementById("city"),out=document.getElementById("out");
async function geocode(q){const r=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(q)}&count=1`);const d=await r.json();if(!d.results?.length)throw new Error("City not found");return d.results[0];}
async function weather(lat,lon){const r=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current=temperature_2m,weather_code,wind_speed_10m`);return r.json();}
f.addEventListener("submit",async(e)=>{e.preventDefault();out.textContent="Loading...";try{const g=await geocode(city.value.trim());const w=await weather(g.latitude,g.longitude);const c=w.current;out.innerHTML=`<h2>${g.name}, ${g.country}</h2><p>Temperature: <strong>${c.temperature_2m}°C</strong></p><p>Wind: <strong>${c.wind_speed_10m} km/h</strong></p><p>Code: <strong>${c.weather_code}</strong></p>`;}catch(err){out.textContent=err.message;}});